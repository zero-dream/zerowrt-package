#!/bin/bash
# Copyright (C) 2000 ZeroDream
# https://github.com/zero-dream

# --------------------------------------------------

cd "$WRT_MainPath/"

# --------------------------------------------------

# CollectArtifact
if [[ "$WRT_TEST" == 'false' && "$WRT_ONLY_CONFIG" == 'false' ]]; then
  packagePath="$ZD_ArtifactUploadPath/Package" && mkdir -p "$packagePath"
  cp -a "$CI_TempPath/CompressPackage/." "$packagePath/"
fi

# --------------------------------------------------

exit 0
